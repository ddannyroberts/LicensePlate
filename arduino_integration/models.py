"""
Arduino Integration models - if needed for future extensions
"""
from django.db import models

# Currently no specific models needed -
# Arduino integration uses existing AccessLog and VehicleStatus models
# from vehicle_detection app